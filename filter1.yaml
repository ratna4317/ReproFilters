resources: 
  webhooks:
    - webhook: IncomingWebhook
      connection: FilterServiceConnection
      filters:
        - path: requestType
          value: a
steps:
- task: PowerShell@2
  inputs:
    targetType: 'inline'
    script: |
      Write-Host ${{ convertToJson(parameters.awetesthook.requestType) }}
      write-Host "Filter 1 pipeline with a value"
